<Window x:Class="TraceMatch.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="发现新版本" Height="420" Width="560"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F7F8F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="发现新版本" FontSize="24" FontWeight="SemiBold" Foreground="#17231E"/>
            <TextBlock x:Name="ReleaseNameText" Margin="0,6,0,0" Foreground="#5B6B62" TextWrapping="Wrap"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,18,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="当前版本：" Foreground="#536059" Margin="0,0,8,6"/>
            <TextBlock x:Name="CurrentVersionText" Grid.Column="1" FontWeight="SemiBold" Margin="0,0,0,6"/>
            <TextBlock Grid.Row="1" Text="最新版本：" Foreground="#536059" Margin="0,0,8,0"/>
            <TextBlock x:Name="LatestVersionText" Grid.Row="1" Grid.Column="1" FontWeight="SemiBold"/>
        </Grid>

        <GroupBox Grid.Row="2" Header="更新说明" BorderBrush="#D8DED6">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="ReleaseBodyText"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         BorderThickness="0"
                         Background="Transparent"
                         Padding="8"
                         VerticalScrollBarVisibility="Auto"/>
            </ScrollViewer>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="稍后再说" Width="96" Height="34" Margin="0,0,8,0" Click="Later_Click"/>
            <Button Content="立即下载" Width="104" Height="34" Background="#1F6F4A" Foreground="White" Click="Download_Click"/>
        </StackPanel>
    </Grid>
</Window>
